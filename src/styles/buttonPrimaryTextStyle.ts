import { colors } from '@src/shared/colors'
import type { TextStyle } from 'react-native'

const buttonPrimaryTextStyle: TextStyle = {
  color: colors.secondary[50],
}

export default buttonPrimaryTextStyle
